# TagCloud 后端服务说明

## 📂 目录结构

```bash
fastify-backend/
├── server.js              # 主入口文件
├── routes/
│   └── ai/
│       ├── index.js       # AI 路由（chat/search/status）
│       └── helpers.js     # AI 工具函数（Prompt 工程）
├── .env.example           # 环境变量模板
├── .gitignore
└── package.json
```

## 🚀 快速启动

```bash
cd fastify-backend
npm install
cp .env.example .env  # 编辑 .env 填入 API Key
npm run dev
```

服务将运行在 `http://localhost:3000`

## 🔌 API 接口

| 接口             | 方法 | 说明             |
| ---------------- | ---- | ---------------- |
| `/api/ai/status` | GET  | 检查 AI 服务状态 |
| `/api/ai/models` | GET  | 获取可用模型列表 |
| `/api/ai/chat`   | POST | 流式聊天（SSE）  |
| `/api/ai/search` | POST | 语义搜索 POI     |
| `/health`        | GET  | 健康检查         |

## 🔧 环境变量

| 变量名         | 必填 | 说明                  |
| -------------- | ---- | --------------------- |
| `MIMO_API_KEY` | 是   | 云端 AI 服务密钥      |
| `LOCAL_LM_API` | 否   | 本地 LM Studio 地址   |
| `PORT`         | 否   | 服务端口（默认 3000） |
