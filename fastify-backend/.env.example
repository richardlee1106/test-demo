# =====================================================
# TagCloud Backend 环境变量配置
# 复制此文件为 .env 并修改相应值
# =====================================================

# 服务器配置
# 注意：Windows Hyper-V 保留了端口 3000-3399，使用 3456 或更高
PORT=3456
HOST=0.0.0.0

# PostgreSQL + PostGIS 配置
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456
POSTGRES_DATABASE=tagcloud

# Milvus 向量数据库配置
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION=poi_embeddings

# 本地 LLM 配置 (LM Studio)
LLM_BASE_URL=http://localhost:1234/v1
LLM_MODEL=qwen3-4b-instruct-2507
LLM_EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5

# 外部 API (可选)
AMAP_API_KEY=your_amap_key_here
